<!DOCTYPE html>
<%- include ('../partials/head', {title: 'MyHelp'}) %>
<body>

  <%- include ('../partials/header') %>
  <main>
   
    
      <div class="main-description-company">
        <h2>Limpieza</h2>
        <p>Tu empresa siempre impecable! Limpieza eficiente que tu empresa merece. </p>
      </div>
      <div class="all-clean-works">
        <% cleaning.forEach(row => { %>
        <div class="clean-company">
          <article>
            <div class="img-clean-company">
              <img src="/img/imgServiciosMyHelp/<%= row.imagen %>" alt="<%= row.imagen %>">
            </div>
          <div class="second-description">
            <h3><%= row.nombre %></h3>
            <h4>Precio: <strong>$<%= row.precio %></strong></h4>
            <p><%= row.descripcion %></p>
          </div>
          </article>
        </div>
        <%}); %>
      </div>

    
      <div class="main-description-company">
        <h2>Desinfección</h2>
        <p>Un ambiente limpio y saludable para tu empresa.</p>
      </div>
      <div class="all-disinfection-works">
        <% disinfection.forEach(row => { %>
        <div class="disinfection-company">
          <article>
            <div class="img-disinfection-company">
              <img src="/img/imgServiciosMyHelp/<%= row.imagen %>" alt="<%= row.imagen %>">
            </div>
          <div class="second-description">
            <h3><%= row.nombre %></h3>
            <h4>Precio: <strong>$<%= row.precio %></strong></h4>
            <p><%= row.descripcion %></p>
          </div>
          </article>
        </div>
        <%}); %>
      </div>
    
  </main>
  <%- include ('../partials/footer') %>
  
</body>
</html>
