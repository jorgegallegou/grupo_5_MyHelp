<!DOCTYPE html>
<%- include ('../partials/head', {title: 'Myhelp Productslist'}) %>
<body>
  <%- include ('../partials/header') %>
  <main>
    <div class="main-description buscar-servicios">
      <h2>Resultado de la busqueda</h2>
      <a href="/"
        ><i class="fa-regular fa-hand-point-left">
          <p>Al home</p>
        </i></a
      >
    </div>
    <div class="all-works">
      <% if (allProducts.length > 0) { %> <% allProducts.forEach(row => { %>
      <div class="clean-home">
        <img
          src="img/imgServiciosMyHelp/<%= row.imagen %>"
          alt="<%= row.imagen %>"
        />
        <h3><%= row.nombre %></h3>

        <div class="second-description">
          <h4>Precio: <strong>$<%= row.precio %></strong></h4>
          <p><%= row.descripcion %></p>
          <a href="/productDetail/<%= row.id %>">
            <button class="request" type="submit">Conoce más!</button></a
          >

          <% if (locals.adminLog) { %>
          <a href="product/edit/<%= row.id %>">
            <button class="request" type="submit">Editar</button></a
          >
          <% } %>
        </div>
      </div>
      <%}); %> <% } else { %>
      <h3>Servicio no encontrado😒</h3>
      <% } %>
    </div>
  </main>
  <%- include ('../partials/footer') %>
</body>
