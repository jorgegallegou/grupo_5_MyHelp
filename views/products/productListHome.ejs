<!DOCTYPE html>
<%- include ('../partials/head', {title: 'Myhelp Productslist'}) %>
<body>
	<%- include ('../partials/header') %>
	<main>
		
			<div class="main-description">
				<h2>Limpieza y Aseo</h2>
				<p>
					Tu hogar impecable, sin esfuerzo! Confía en nuestra excelencia en servicios de limpieza.
				</p>
			</div>
			<div class="all-works">
				<% clean.forEach(row => { %>
				<div class="clean-home">
					<article>
						<div class="img-clean">
							<img src="img/imgServiciosMyHelp/<%= row.imagen %>" alt="<%= row.imagen %>" />
						</div>
						<div class="second-description">
							<h3><%= row.nombre %></h3>
							<h4>Precio: <strong>$<%= row.precio %></strong></h4>
							<p><%= row.descripcion %></p>
						</div>
					</article>
				</div>
				<%}); %>
			</div>
		
		
			<div class="main-description">
				<h2>Servicios Especiales</h2>
				<p>Tu necesidad, nuestra especialidad.</p>
			</div>
			<div class="all-works">
				<% special.forEach(row => { %>
				<div class="works">
					<article>
						<div class="img-clean">
							<img src="img/imgServiciosMyHelp/<%= row.imagen %>" alt="<%= row.imagen %>" />
						</div>
						<div class="second-description">
							<h3><%= row.nombre %></h3>
							<h4>Precio: <strong>$<%= row.precio %></strong></h4>
							<p><%= row.descripcion %></p>
						</div>
					</article>
				</div>
				<%}); %>
			</div>
	
	</main>
	<%- include ('../partials/footer') %>
</body>
